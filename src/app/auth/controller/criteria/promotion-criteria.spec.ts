import { PromotionCriteria } from '../../../controller/criteria/promotion-criteria';

describe('PromotionCriteria', () => {
  it('should create an instance', () => {
    expect(new PromotionCriteria()).toBeTruthy();
  });
});
